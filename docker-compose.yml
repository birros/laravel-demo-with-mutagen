version: '2'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        PHP_VERSION: ${PHP_VERSION}
        COMPOSER_VERSION: ${COMPOSER_VERSION}
    volumes:
      - laravel-demo-code:/var/www/html
    networks:
      - reverse-proxy
      - db
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.${TRAEFIK_APP_NAME}.entrypoints=https"
      - "traefik.http.routers.${TRAEFIK_APP_NAME}.tls=true"
      - "traefik.http.routers.${TRAEFIK_APP_NAME}.rule=Host(`${TRAEFIK_HOST}`)"

  db:
    image: mariadb
    networks:
      - db
    volumes:
      - laravel-demo-db:/var/lib/mysql
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
      MYSQL_DATABASE: ${DB_DATABASE}
      MYSQL_USER: ${DB_USERNAME}
      MYSQL_PASSWORD: ${DB_PASSWORD}

networks:
  reverse-proxy:
    external: true
  db:

volumes:
  laravel-demo-code:
  laravel-demo-db:

x-mutagen:
  sync:
    defaults:
      ignore:
        vcs: true
    laravel-demo-code:
      alpha: .
      beta: volume://laravel-demo-code
      permissions:
        defaultFileMode: 666
        defaultDirectoryMode: 777
      ignore:
        paths:
          - "/storage/app/db"
          - "/vendor"
          - "/node_modules"
          - "/.composer"
          - "/.npm"
      configurationBeta:
        permissions:
          defaultOwner: "id:33" # www-data
          defaultGroup: "id:33" # www-data
    laravel-demo-db:
      alpha: ./storage/app/db
      beta: volume://laravel-demo-db
      permissions:
        defaultFileMode: 666
        defaultDirectoryMode: 777
      ignore:
        paths:
          - "/*"
          - "!/storage/app/db"
      configurationBeta:
        permissions:
          defaultOwner: "id:33" # www-data
          defaultGroup: "id:33" # www-data
